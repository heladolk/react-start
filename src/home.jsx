import React from 'react'

export class Home extends React.Component {
  render () {
    return (
      <div className="home">
        React + Webpack + Babel + Jest + Spring Starter kit
      </div>
    );
  }
}
